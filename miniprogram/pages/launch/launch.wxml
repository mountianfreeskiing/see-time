<!--pages/launch/launch.wxml-->
<view class="launch">
  
  <image class="launch-animate" src="../../images/launch.gif" />

  <button wx:if="{{!hasUserInfo && canIUse}}" class="launch-button" open-type="getUserInfo" bindgetuserinfo="getUserInfo">立即体验</button>
</view>
